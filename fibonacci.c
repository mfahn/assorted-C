#include <stdio.h>
#include <strings.h>
#include <stdlib.h>

int main ()
{
    int x, a[100], len, fib;

    len=strlen(a);
    for(x=0; x<len; x++)
    {
        printf("%d\n", a[x]);
    }


    return 0;
}


