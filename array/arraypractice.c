#include <stdio.h>
#include <stdlib.h>
#include <time.h>

int main()

{

int even, odd, line, numlist[25];

srand(time(NULL));

for(line=0; line<=10; line++)
{
	numlist=rand() % 100 +1;
	
	if(numlist=10)
		line 
}

if(numlist

return 0;
}
